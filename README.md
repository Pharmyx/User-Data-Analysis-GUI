# User-Data-Analysis-GUI



A Python application that creates a SQLite database (`user_data.db`) 
with sample user data and displays analysis (total users, average age, user details) in a Tkinter GUI.
